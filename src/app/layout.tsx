import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Solide Intelligence | Trading Dashboard',
  description: 'Multi-source trading intelligence with Reddit sentiment, TVL monitoring, news analysis, and time series forecasting',
  keywords: ['trading', 'crypto', 'sentiment analysis', 'DeFi', 'Hyperliquid'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>
        <div className="min-h-screen bg-background">
          {children}
        </div>
      </body>
    </html>
  );
}
